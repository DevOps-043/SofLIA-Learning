Command: npm run type-check --workspace=apps/web
Date: 2026-05-18
Status: TIMEOUT

Summary:
- Official npm script still exceeds the local execution window and can leave a child `tsc --noEmit -p tsconfig.typecheck.json` process alive.
- A direct full compiler run also exceeded 300 seconds without emitting diagnostics.
- Temporary process trees were killed after each timeout to avoid background CPU contention.

Observed stdout before timeout:

> @aprende-y-aplica/web@1.0.0 type-check
> tsc --noEmit -p tsconfig.typecheck.json

Observed stderr before timeout:

(empty)

Isolation notes:
- `src/core/**` completes in about 12 s.
- `src/lib/**` completes in about 7 s.
- `src/shared/**` completes in about 5 s.
- `src/app/**` still exceeds 90 s as a slice.
- `src/features/**` still exceeds 90 s as a slice.
- `apps/web/src/app/api/admin/courses/[id]/student-details/[userId]/route.ts` was reduced from a local slice timeout to about 4 s by using `fromLoose<T>()` and explicit row contracts.

P3 follow-up slice notes:
- `app/api/admin`, `app/api/business`, `app/api/[orgSlug]`, `app/api/courses`, `app/api/study-planner`, `features/admin`, and `features/courses` still exceeded 75 s with temporary configs extending `tsconfig.typecheck.json`.
- `features/business-panel` initially emitted concrete TS errors after about 44 s. The low-risk `UserDropdown` nullability, missing `SELECT_COLUMNS` import, and `BusinessInviteUserModal` translator typing issues were remediated. A follow-up slice now reaches the next blocker set: Joyride style/step typing, `BusinessLearningPathsPage` style incompatibilities, hierarchy node-dashboard contract drift, `TeamMembersModal` relative import drift, `SSOSettingsCard` boolean strictness, and business-users/stats service contract drift.
- `features/study-planner` emitted concrete TS errors after about 46 s, including dashboard assistant ref typing, missing `scheduleStudyPlannerRedirect` in LIA logic sections, state setter variance, and duplicate `CalendarEvent` types between hook and calendar modules.
- A focal production type-check for user-groups routes timed out at 90 s under the same TD-001 process contention. Tests and ESLint for the user-groups schema/route batch passed.

Current blocker:
TD-001 remains open for the full workspace command. Do not set `ignoreBuildErrors: false` until the official `npm run type-check --workspace=apps/web` completes reliably without manual process cleanup.
