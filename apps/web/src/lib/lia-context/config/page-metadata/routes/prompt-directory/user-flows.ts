import type { UserFlow } from '../../types';

export const promptDirectoryUserFlows: UserFlow[] = [
      {
        name: 'Buscar y usar prompt',
        steps: ['1. Buscar por categoría o texto', '2. Ver prompt', '3. Copiar o guardar en favoritos'],
        commonBreakpoints: []
      }
    ];
