export type CertificateHashImmutableFunction = {
  Args: {
    p_certificate_id: string
    p_certificate_url: string
    p_course_id: string
    p_enrollment_id: string
    p_issued_at: string
    p_user_id: string
  }
  Returns: string
}
