export const NOTIFICATION_SELECT = `
  notification_id,
  user_id,
  notification_type,
  title,
  message,
  metadata,
  priority,
  status,
  channels_sent,
  channels_pending,
  read_at,
  expires_at,
  organization_id,
  group_id,
  created_at,
  updated_at
`
