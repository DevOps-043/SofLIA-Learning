export const COURSE_INFO_SELECT = `
  id,
  title,
  description,
  slug,
  category,
  level,
  instructor_id,
  thumbnail_url,
  duration_total_minutes,
  is_active,
  price,
  average_rating,
  student_count
`

export const PERSON_NAME_SELECT = `
  display_name,
  first_name,
  last_name
`
