/**
 * Legacy compatibility wrapper.
 *
 * The live implementation was already split into `lia-context.service.ts`
 * and companion modules. Keep this file only to preserve old import paths.
 */

export {
  SofLIAContextService,
  type StudyPlannerContext,
} from './lia-context.service';
